class Face:
    @staticmethod
    def face_detection(image):
        """
        Method that detects faces in an image

        Keyword arguments:
        image -- (django.core.files.uploadedfile.InMemoryUploadedFile) The image to be analyzed
        Returns:
        int -- The number of faces detected
        """
        return 1  # for testing
